Thiết kế proto file chi tiết --

k6 test cho gRPC latency --

Grafana dashboard cho latency --

Vẽ architecture diagram chi tiết

Architecture diagram (REST + gRPC flow)

README section giải thích gRPC decision

Sample Node.js gRPC server/client code

Prometheus metrics code (Node.js + prom-client)

Export sẵn Grafana dashboard JSON

Alert rule (latency > threshold)

End-to-end load test + dashboard analysis

Viết System Design Document (SDD)

Viết Postmortem giả lập (khi Redis down)

Viết Scaling plan: 10x traffic

Chuẩn hóa interview talking points
